import pandas as pd

# The first notebook is Data Exploration, nothing to extract here that is needed.

# Third notebook is Data Analysis so nothing to extract here either.

# From notebook 2 I will extract the preprocessing steps into one function.
def preprocess_data(X: pd.DataFrame):
    pass

# From notebook 4 I will extract everything into one function and maybe rewrite some code a little to follow some good practices.
def manipulate_features(X: pd.DataFrame):
    pass